services:
  dialog-analyze:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_API_URL: ${OPENAI_API_URL}
